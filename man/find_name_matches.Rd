% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_name_matches.R
\name{find_name_matches}
\alias{find_name_matches}
\title{Identify Potential Beneficiary Name Matches From a Master Dataset}
\usage{
find_name_matches(
  flagged_data,
  master_data,
  flagged_col = "ben_name",
  master_col = "ben_name",
  max_dist = 3
)
}
\arguments{
\item{flagged_data}{A data frame containing the flagged records needing verification.}

\item{master_data}{A data frame containing the verified master benchmark records.}

\item{flagged_col}{The bare or quoted column name of the name column in \code{flagged_data}. Defaults to "ben_name".}

\item{master_col}{The bare or quoted column name of the name column in \code{master_data}. Defaults to "ben_name".}

\item{max_dist}{Integer specifying the maximum allowed string distance/edits to qualify as a match. Defaults to 3.}
}
\value{
A data frame matching the structure of \code{flagged_data}, with an additional
\code{name_possible} character column containing the matched master dataset name (or NA if no match is found).
}
\description{
Compares a column of names in a flagged dataset against a column of names in a
master reference dataset using string distance. If a potential match is identified
within a specified text edit threshold, the candidate name from the master dataset
is written to a new column named \code{name_possible} in the flagged dataset.
}
\examples{
\dontrun{
# create sample flagged data with typos
bad_data <- data.frame(
  uuid = c("id_1", "id_2", "id_3"),
  ben_name = c("Jhn Doe", "Jane Smithh", "Xavier Mery"),
  stringsAsFactors = FALSE
)

# create master reference data
good_data <- data.frame(
  ben_name = c("John Doe", "Jane Smith", "Robert Chen"),
  stringsAsFactors = FALSE
)

# match names
verified_df <- find_name_matches(bad_data, good_data, max_dist = 2)
}
}
