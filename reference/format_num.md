# format numeric vectors using gtsummary style number labels safely

format numeric vectors using gtsummary style number labels safely

## Usage

``` r
format_num(x, ...)
```

## Arguments

- x:

  numeric vector to be formatted

- ...:

  optional arguments passed along to label_style_number

## Value

a character vector of formatted numbers
