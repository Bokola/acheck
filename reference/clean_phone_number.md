# clean phone number by adding 254 prexif

clean phone number by adding 254 prexif

## Usage

``` r
clean_phone_number(x, col = c("reg_num", "telephone_number", "proxy_num"))
```

## Arguments

- x:

  dataframe

- col:

  character vector of phone number columns

## Value

dataframe
