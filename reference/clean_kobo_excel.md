# clean manual kobo excel data to align with api structure

clean manual kobo excel data to align with api structure

## Usage

``` r
clean_kobo_excel(data)
```

## Arguments

- data:

  data.frame. the raw excel data frame (ddd)

## Value

a cleaned data frame matching the transformed api structure

## Examples

``` r
if (FALSE) clean_kobo_excel(ddd) # \dontrun{}
```
