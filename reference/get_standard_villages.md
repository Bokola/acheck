# separate villages collapsed by forward slash ("\|")

separate villages collapsed by forward slash ("\|")

## Usage

``` r
get_standard_villages(data1 = df_partner, data2 = df_clean)
```

## Arguments

- data1:

  dataframe. ESR/partner data

- data2:

  dataframe. registration data

## Value

a dataframe

## Examples

``` r
if (FALSE) get_standard_villages(data1 = df_partner, data2 = df_clean) # \dontrun{}
```
